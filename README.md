A simple xUnit (and later more) uploader


Detect which CI system is used, traverse the filesystem and try to
find xUnit XML files. Finally upload them to the bob-bench system
for analysis and storage.
